maf_assignment <- maf_object@clinical.data
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis== '[Not Available]', F,T)
cutoff_mask <- ifelse(maf_assignment$height_cm_at_diagnosis >= 170 , T,F)
maf_assignment <- maf_assignment[cutoff_mask,]
maf_assignment <- maf_assignment[not_available_mask,]
maf_assignment <- maf_object@clinical.data
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
cutoff_mask <- ifelse(maf_assignment$height_cm_at_diagnosis >= 170 , T,F)
maf_assignment <- maf_assignment[cutoff_mask,]
maf_assignment <- maf_object@clinical.data
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
cutoff_mask <- ifelse(maf_assignment$height_cm_at_diagnosis >= 170 , T,F)
maf_assignment <- maf_assignment[cutoff_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
maf_assignment <- maf_object@clinical.data
cutoff_mask <- ifelse(maf_assignment$height_cm_at_diagnosis >= 170 , T,F)
maf_assignment <- maf_assignment[cutoff_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
maf_assignment <- maf_object@clinical.data
cutoff_mask <- ifelse(maf_assignment$height_cm_at_diagnosis >= 170 , T,F)
maf_assignment <- maf_assignment[cutoff_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
maf_assignment <- maf_object@clinical.data
cutoff_mask <- ifelse(maf_assignment$height_cm_at_diagnosis >= 175 , T,F)
maf_assignment <- maf_assignment[cutoff_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
maf_assignment <- maf_object@clinical.data
cutoff_mask <- ifelse(maf_assignment$height_cm_at_diagnosis >= 185 , T,F)
maf_assignment <- maf_assignment[cutoff_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
maf_height <- maf_object@clinical.data
cutoff_mask <- ifelse(maf_height$height_cm_at_diagnosis >= 185 , T,F)
maf_height <- maf_height[cutoff_mask,]
maf_height$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
View(maf_height)
View(maf_height)
maf_assignment <- maf_object@clinical.data
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask,]
View(maf_assignment)
View(maf_assignment)
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis== 'NA', F,T)
maf_assignment <- maf_assignment[not_available_mask,]
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask,]
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis == '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask,]
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis == '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask,]rep(...,length=.N)
maf_clinical <- maf_object@clinical.data
na_mask <- ifelse(maf_clinical$age_at_diagnosis== '[Not Available]', F,T)
maf_clinical <- maf_clinical[na_mask,]
maf_clinical$age_at_diagnosis <- as.numeric(maf_clinical$age_at_diagnosis)
median_age <- median(maf_clinical$age_at_diagnosis)
print(median_age)
maf_clinical$age_category <- ifelse(maf_clinical$age_at_diagnosis > median_age, 'Old','Young')
young_patient_barcodes <- maf_clinical[maf_clinical$age_category == 'Young', c("Tumor_Sample_Barcode")]
young_patient_barcodes <- young_patient_barcodes$Tumor_Sample_Barcode
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis == '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask, ]
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis == '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask, ]
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis == '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask, ]
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis == '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask, ]
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis == '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask, ]
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis == '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask, ]
maf_assignment <- maf_object@clinical.data
not_available_mask <- ifelse(maf_clinical$height_cm_at_diagnosis == '[Not Available]', F,T)
maf_assignment <- maf_assignment[not_available_mask, ]
maf_clinical <- maf_object@clinical.data
na_mask <- ifelse(maf_clinical$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_clinical <- maf_clinical[na_mask,]
maf_clinical$height_cm_at_diagnosis <- as.numeric(maf_clinical$height_cm_at_diagnosis)
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
maf_clinical <- maf_object@clinical.data
na_mask <- ifelse(maf_clinical$age_at_diagnosis== '[Not Available]', F,T)
maf_clinical <- maf_clinical[na_mask,]
maf_clinical$age_at_diagnosis <- as.numeric(maf_clinical$age_at_diagnosis)
median_age <- median(maf_clinical$age_at_diagnosis)
print(median_age)
maf_clinical$age_category <- ifelse(maf_clinical$age_at_diagnosis > median_age, 'Old','Young')
young_patient_barcodes <- maf_clinical[maf_clinical$age_category == 'Young', c("Tumor_Sample_Barcode")]
young_patient_barcodes <- young_patient_barcodes$Tumor_Sample_Barcode
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_hieght <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_hieght <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_clinical$age_category <- ifelse(maf_clinical$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
Short_patient_barcodes <- maf_clinical[maf_clinical$height_cm_at_diagnosis == 'Short', c("Tumor_Sample_Barcode")]
Short_patient_barcodes <- Short_patient_barcodes$Tumor_Sample_Barcode
maf_clinical <- maf_object@clinical.data
na_mask <- ifelse(maf_clinical$age_at_diagnosis== '[Not Available]', F,T)
maf_clinical <- maf_clinical[na_mask,]
maf_clinical$age_at_diagnosis <- as.numeric(maf_clinical$age_at_diagnosis)
median_age <- median(maf_clinical$age_at_diagnosis)
print(median_age)
maf_clinical$age_category <- ifelse(maf_clinical$age_at_diagnosis > median_age, 'Old','Young')
young_patient_barcodes <- maf_clinical[maf_clinical$age_category == 'Young', c("Tumor_Sample_Barcode")]
young_patient_barcodes <- young_patient_barcodes$Tumor_Sample_Barcode
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_clinical$age_category <- ifelse(maf_clinical$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
Short_patient_barcodes <- maf_clinical[maf_clinical$height_cm_at_diagnosis == 'Short', c("Tumor_Sample_Barcode")]
Short_patient_barcodes <- Short_patient_barcodes$Tumor_Sample_Barcode
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_clinical$age_category <- ifelse(maf_clinical$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_assignment$height_category <- ifelse(maf_clinical$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_assignment$height_category <- ifelse(maf_clinical$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_assignment$height_category <- ifelse(maf_assignment$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
Short_patient_barcodes <- maf_assignmentl[maf_assignment$height_cm_at_diagnosis == 'Short', c("Tumor_Sample_Barcode")]
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_assignment$height_category <- ifelse(maf_assignment$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
Short_patient_barcodes <- maf_assignment[maf_assignment$height_cm_at_diagnosis == 'Short', c("Tumor_Sample_Barcode")]
Short_patient_barcodes <- Short_patient_barcodes$Tumor_Sample_Barcode
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_assignment$height_category <- ifelse(maf_assignment$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
Short_patient_barcodes <- maf_assignment[maf_assignment$height_cm_at_diagnosis == 'Short', c("Tumor_Sample_Barcode")]
Short_patient_barcodes <- Short_patient_barcodes$Tumor_Sample_Barcode
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_assignment$height_category <- ifelse(maf_assignment$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
Short_patient_barcodes <- maf_assignment[maf_assignment$height_cm_at_diagnosis == 'Short', c("Tumor_Sample_Barcode")]
Short_patient_barcodes <- Short_patient_barcodes$Tumor_Sample_Barcode
Tall_patient_barcodes <- maf_assignment[maf_assignment$height_cm_at_diagnosis == 'Tall', c("Tumor_Sample_Barcode")]
Tall_patient_barcodes <- Tall_patient_barcodes$Tumor_Sample_Barcode
Short_patient_barcodes <- maf_assignment[maf_assignment$height_cm_at_diagnosis == 'Short', c("Tumor_Sample_Barcode")]
Short_patient_barcodes <- Short_patient_barcodes$Tumor_Sample_Barcode
Tall_patient_barcodes <- maf_assignment[maf_assignment$height_cm_at_diagnosis == 'Tall', c("Tumor_Sample_Barcode")]
Tall_patient_barcodes <- Tall_patient_barcodes$Tumor_Sample_Barcode
Short_maf <- subsetMaf(maf = maf_object,
tsb = short_patient_barcodes)
Short_patient_barcodes <- maf_assignment[maf_assignment$height_cm_at_diagnosis == 'Short', c("Tumor_Sample_Barcode")]
Short_patient_barcodes <- Short_patient_barcodes$Tumor_Sample_Barcode
Tall_patient_barcodes <- maf_assignment[maf_assignment$height_cm_at_diagnosis == 'Tall', c("Tumor_Sample_Barcode")]
Tall_patient_barcodes <- Tall_patient_barcodes$Tumor_Sample_Barcode
Short_maf <- subsetMaf(maf = maf_object,
tsb = Short_patient_barcodes)
Short_patient_barcodes <- maf_assignment[maf_assignment$height_category == 'Short', c("Tumor_Sample_Barcode")]
Short_patient_barcodes <- Short_patient_barcodes$Tumor_Sample_Barcode
Tall_patient_barcodes <- maf_assignment[maf_assignment$height_category == 'Tall', c("Tumor_Sample_Barcode")]
Tall_patient_barcodes <- Tall_patient_barcodes$Tumor_Sample_Barcode
Short_maf <- subsetMaf(maf = maf_object,
tsb = Short_patient_barcodes)
Tall_maf <- subsetMaf(maf = maf_object,
tsb = Tall_patient_barcodes)
coOncoplot(m1 = Short_maf,
m2 = Tall_maf,
m1Name = "Short",
m2Name = "Tall",
borderCol = NA)
knitr::opts_knit$set(root.dir = normalizePath("/Users/daven/Documents/qbio_490_daven/analysis_data"))
library(BiocManager)
library(TCGAbiolinks)
library(maftools)
clinical <- read.csv("/Users/daven/Documents/qbio_490_daven/analysis_data/skcm_clinical_data.csv")
colnames(clinical)[ colnames(clinical) == "bcr_patient_barcode" ] <- "Tumor_Sample_Barcode"
write.csv(clinical, "/Users/daven/Documents/qbio_490_daven/analysis_data/skcm_clinical_data.csv", row.names = FALSE)
maf_query <- GDCquery(
project = "TCGA-SKCM",
data.category = "Simple Nucleotide Variation",
access = "open", # we only have access to somatic mutations which are open access
data.type = "Masked Somatic Mutation",
workflow.type = "Aliquot Ensemble Somatic Variant Merging and Masking"
)
#GDCdownload(maf_query) # you shouldn't have to redownload any data!
maf <- GDCprepare(maf_query) # as long as it runs, ignore any errors
TTN_mask <- maf.data$Hugo_Symbol == 'TTN'
TTN_patients <- maf.data[TTN_mask, 'Tumor_Sample_Barcode']
TTN_mask <- maf.data$Hugo_Symbol == 'TTN'
TTN_patients <- maf.data[TTN_mask, 'Tumor_Sample_Barcode']
maf_assignment$TTN_status <- ifelse(maf_assignment$Tumor_Sample_Barcode %in% TTN_patients, 'Positive', 'Negative')
table(maf_assignment$TTN_status)
View(maf_assignment)
TTN_mask <- maf.data$Hugo_Symbol == 'TTN'
TTN_patients <- maf.data[TTN_mask, 'Tumor_Sample_Barcode']
maf_assignment$TTN_status <- ifelse(maf_assignment$Tumor_Sample_Barcode %in% TTN_patients, 'Positive', 'Negative')
cont_table <- table(maf_assignment$TTN_status, maf_assignment$height_category)
TTN_mask <- maf.data$Hugo_Symbol == 'TTN'
TTN_patients <- maf.data[TTN_mask, 'Tumor_Sample_Barcode']
maf_assignment$TTN_status <- ifelse(maf_assignment$Tumor_Sample_Barcode %in% TTN_patients, 'Positive', 'Negative')
cont_table <- table(maf_assignment$TTN_status, maf_assignment$height_category)
mosaicplot(cont_table)
TTN_mask <- maf.data$Hugo_Symbol == 'TTN'
TTN_patients <- maf.data[TTN_mask, 'Tumor_Sample_Barcode']
maf_assignment$TTN_status <- ifelse(maf_assignment$Tumor_Sample_Barcode %in% TTN_patients, 'Positive', 'Negative')
cont_table <- table(maf_assignment$TTN_status, maf_assignment$height_category)
mosaicplot(cont_table)
fisher.test(cont_table)
lollipopPlot(maf=maf_object
gene = "TTN")
lollipopPlot(maf= maf_object
gene = "TTN")
lollipopPlot(maf= maf_object,
gene = "TTN")
lollipopPlot(maf= maf_assignment$height_category,
gene = "TTN")
height_subset <- subset(maf = maf_assignment,
height_category)
View(maf_assignment)
View(maf_assignment)
lollipopPlot(maf= Tall_maf,
gene = "TTN")
lollipopPlot(maf= Short_maf,
gene = "TTN")
height_maf <- subset(maf = maf_assignment,
tsb = height_cm_at_diagnosis)
lollipopPlot(maf= maf_assignment,
gene = "TTN")
lollipopPlot(maf= maf_object,
gene = "TTN")
height_subset <- subset(maf_assignment, maf_assignment$height_cm_at_diagnosis)
height_subset <- subset(maf_assignment$height_cm_at_diagnosis)
height_subset <- subset(maf_assignment, $height_cm_at_diagnosis)
height_subset <- subset(maf_assignment, height_cm_at_diagnosis)
lollipopPlot(maf= maf_object,
gene = "TTN")
lollipopPlot(maf= maf_assignment,
gene = "TTN")
lollipopPlot(maf= Short_maf,
gene = "TTN")
Height_subset <- subsetMaf(maf = maf_assignment,
)
Height_subset <- subsetMaf(maf = maf_assignment,
gene = "TTN")
Height_subset <- subsetMaf(maf = maf_assignment,
gene = "TTN")
Height_subset <- subsetMaf(maf = maf_assignment,
tsb = height_cm_at_diagnosis)
Height_subset <- subsetMaf(maf = maf_assignment,
tsb = height_cm_at_diagnosis)
Height_subset <- subsetMaf(maf = maf_assignment,
height_cm_at_diagnosis != "NA" )
Height_subset <- subsetMaf(maf = maf_assignment,
height_cm_at_diagnosis != "NA" )
Height_subset <- subsetMaf(maf = maf_assignment,
height_cm_at_diagnosis != "NA" )
Height_subset <- subsetMaf(maf = maf_assignment,
height_cm_at_diagnosis != "NA" )
Height_subset <- subsetMaf(maf = maf_assignment,
height_cm_at_diagnosis != "NA" )
Height_subset <- subsetMaf(maf = maf_assignment,
tsb =  )
)
lollipopPlot(maf= maf_object,
gene = "TTN")
lollipopPlot(maf= maf_object,
gene = "TTN")
lollipopPlot2(m1 = Short_maf,
m2 = Tall_maf,
m1_name = "Short",
m2_name = "Tall",
gene = "TTN")
lollipopPlot2(m1 = Short_maf,
m2 = Tall_maf,
m1_name = "Short",
m2_name = "Tall",
gene = "TTN")
View(maf_object)
View(maf_object)
View(maf.data)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$last_contact_days_to == "[Not Available]", cleaned_clinic$last_contact_days_to, cleaned_clinic$death_days_to)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$last_contact_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = T)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$last_contact_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]"|maf_assignment$last_contact_days_to=='[Completed]', maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$last_contact_days_to == "[Not Available]"|maf_assignment$last_contact_days_to=='[Completed]', maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
Overall_Survival_Status <- as.numeric(maf_assignment$days_to_last_followup)
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
Overall_Survival_Status <- as.numeric(maf_assignment$days_to_last_followup)
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
days_to_last_followup <- as.numeric(maf_assignment$days_to_last_followup)
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
days_to_last_followup <- as.numeric(maf_assignment$days_to_last_followup)
maf_assignment$Overall_Survival_Status <- ifelse(maf_assignment$vital_status=="Alive",F,T )
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
days_to_last_followup <- as.numeric(maf_assignment$days_to_last_followup)
maf_assignment$Overall_Survival_Status <- ifelse(maf_assignment$vital_status=="Alive",F,T )
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
Status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
days_to_last_followup <- as.numeric(maf_assignment$days_to_last_followup)
maf_assignment$Overall_Survival_Status <- ifelse(maf_assignment$vital_status=="Alive",F,T )
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
Status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
maf_assignment$Overall_Survival_Status <- ifelse(maf_assignment$vital_status=="Alive",F,T )
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
Status = "Overall_Survival_Status",
isTCGA = TRUE)
View(maf_assignment)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
maf_assignment <- as.numeric(maf_assignment$days_to_last_followup)
maf_assignment$Overall_Survival_Status <- ifelse(maf_assignment$vital_status=="Alive",F,T )
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
maf_assignment[, 'days_to_last_followup']
maf_assignment['days_to_last_followup']
maf_assignment
maf_assignment
maf_assignment <- maf_object@clinical.data
na_mask <- ifelse(maf_assignment$height_cm_at_diagnosis== '[Not Available]', F,T)
maf_assignment <- maf_assignment[na_mask,]
maf_assignment$height_cm_at_diagnosis <- as.numeric(maf_assignment$height_cm_at_diagnosis)
median_height <- median(maf_assignment$height_cm_at_diagnosis)
print(median_height)
maf_assignment$height_category <- ifelse(maf_assignment$height_cm_at_diagnosis > median_hieght, 'Tall','Short')
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
maf_assignment$days_to_last_followup <- as.numeric(maf_assignment[, 'days_to_last_followup'])
Short_patient_barcodes <- maf_assignment[maf_assignment$height_category == 'Short', c("Tumor_Sample_Barcode")]
Short_patient_barcodes <- Short_patient_barcodes$Tumor_Sample_Barcode
Tall_patient_barcodes <- maf_assignment[maf_assignment$height_category == 'Tall', c("Tumor_Sample_Barcode")]
Tall_patient_barcodes <- Tall_patient_barcodes$Tumor_Sample_Barcode
Short_maf <- subsetMaf(maf = maf_object,
tsb = Short_patient_barcodes)
Tall_maf <- subsetMaf(maf = maf_object,
tsb = Tall_patient_barcodes)
coOncoplot(m1 = Short_maf,
m2 = Tall_maf,
m1Name = "Short",
m2Name = "Tall",
borderCol = NA)
## Large discrepancy in the TTN
## The TTN gene is used for encoding titin, which is a muscle protein that stabilizes the sarcomeres of the body to prevent them from overstretching
## I am not too sure why TTN has a high mutation rate - being tall should not change how your DNA functions and mutates, but maybe the pvalue will say something different
TTN_mask <- maf.data$Hugo_Symbol == 'TTN'
TTN_patients <- maf.data[TTN_mask, 'Tumor_Sample_Barcode']
maf_assignment$TTN_status <- ifelse(maf_assignment$Tumor_Sample_Barcode %in% TTN_patients, 'Positive', 'Negative')
cont_table <- table(maf_assignment$TTN_status, maf_assignment$height_category)
mosaicplot(cont_table)
fisher.test(cont_table)
##The P-value given indicates there is no significant correlation between height and the rate of mutation in the TTN gene
lollipopPlot2(m1 = Short_maf,
m2 = Tall_maf,
m1_name = "Short",
m2_name = "Tall",
gene = "TTN")
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
#maf_assignment$days_to_last_followup <- as.numeric(maf_assignment[, 'days_to_last_followup'])
maf_assignment$Overall_Survival_Status <- ifelse(maf_assignment$vital_status=="Alive",F,T )
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
Status = "Overall_Survival_Status",
isTCGA = TRUE)
as.numeric(maf_assignment[, 'days_to_last_followup'])
table(maf_assignment$days_to_last_followup)
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
maf_assignment <- maf_assignment[maf_assignment$days_to_last_followup != '[Completed'], )
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
maf_assignment <- maf_assignment[maf_assignment$days_to_last_followup != '[Completed'], ]
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
maf_assignment <- maf_assignment[maf_assignment$days_to_last_followup != '[Completed]', ]
maf_assignment$Overall_Survival_Status <- ifelse(maf_assignment$vital_status=="Alive",F,T )
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
Status = "Overall_Survival_Status",
isTCGA = TRUE)
maf_object@clinical.data
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
maf_assignment <- maf_assignment[maf_assignment$days_to_last_followup != '[Completed]', ]
maf_assignment$Overall_Survival_Status <- ifelse(maf_assignment$vital_status=="Alive",F,T )
maf_object@clinical.data <- maf_assignment
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
Status = "Overall_Survival_Status",
isTCGA = TRUE)
?mafSurvival
maf_assignment$days_to_last_followup <- ifelse(maf_assignment$death_days_to == "[Not Applicable]" |maf_assignment$death_days_to == "[Not Available]", maf_assignment$last_contact_days_to, maf_assignment$death_days_to)
maf_assignment <- maf_assignment[maf_assignment$days_to_last_followup != '[Completed]', ]
maf_assignment$Overall_Survival_Status <- ifelse(maf_assignment$vital_status=="Alive",F,T )
maf_object@clinical.data <- maf_assignment
mafSurvival(maf = maf_object,
genes = "TTN",
time = "days_to_last_followup",
Status = "Overall_Survival_Status",
clinicalData = maf_assignment,
isTCGA = TRUE)
